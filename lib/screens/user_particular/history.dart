import 'package:flutter/material.dart';

class UserParticularHistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("history"),
    );
  }
}
