import 'package:flutter/material.dart';

class UserParticularEntryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Entry Page"),
    );
  }
}
