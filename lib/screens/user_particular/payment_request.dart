import 'package:flutter/material.dart';

class UserParticularPaymentConfirmationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Payment confirmation"),
    );
  }
}
