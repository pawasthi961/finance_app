import 'package:flutter/material.dart';

class UserParticularEntryRequestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        "Entry Request Page",
      ),
    );
  }
}
